# Smoke-allowlist do Lumen (AGENTE D, 2026-09-13).
# Formato: `<caminho relativo à raiz>: <motivo curto>`.
# Arquivos aqui falham no front-end atual por regras em migração ativa
# (dono: compilador/exemplos). O smoke os relata como [XFAIL] e não
# quebra o CI; qualquer OUTRA falha quebra. Verificação estrita:
#   python3 tools/test/check_examples.py --strict
examples/001-050/022_ordenacao.lum: borrowck v0.2-lite é flow-insensível: `return v` antecipado invalida usos posteriores de `v` no mesmo escopo
examples/001-050/024_grafo_bfs.lum: idem (`return g` antecipado invalida usos posteriores de `g`)
examples/001-050/044_regras.lum: semantica ainda não registra variantes de enum no escopo (`Alta`/`Media`/`Baixa` como construtores)
